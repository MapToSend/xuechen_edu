<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.online.edu.ucservice.mapper.UcenterMemberMapper">


    <select id="queryUcenter" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM ucenter_member uc WHERE DATE(uc.`gmt_create`)=#{date}

--         SELECT COUNT(*) FROM ucenter_member
    </select>
</mapper>
